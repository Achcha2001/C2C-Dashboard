services:
  - type: web
    name: mmbl-analytics
    env: python
    buildCommand: pip install --no-cache-dir -r requirements.txt
    startCommand: gunicorn -k gthread -w 2 -t 120 -b 0.0.0.0:$PORT app:app
    autoDeploy: true
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
